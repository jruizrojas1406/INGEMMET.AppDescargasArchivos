@section Scripts {
    <script type="text/javascript">
        (function ($) {
            //var size;
            //$.ajax({
            //    url: 'api/FileApi?IsLengthOnly=true',
            //    async: false,
            //    success: function (data) {
            //        size = parseInt(data);
            //        alert(size);
            //    }
            //});

            //var totalThreads = 4;
            //var maxSize = parseInt(size / totalThreads, 10) + (size % totalThreads > 0 ? 1 : 0);
            //var ajaxRequests = [];
            //var results = [];
            //function reqToServer(reqNo) {
            //    var range = "bytes=" + (reqNo * maxSize) + "-" + ((reqNo + 1) * maxSize - 1);
            //    return $.ajax({
            //        url: 'api/FileApi?IsLengthOnly=false',
            //        headers: { Range: range },
            //        success: function (data) {
            //            results[reqNo] = data;
            //        }
            //    });
            //}

            //for (var i = 0; i < totalThreads; i++) {
            //    ajaxRequests.push(reqToServer(i));
            //}

            //console.log(ajaxRequests);
        })(jQuery);
    </script>
}